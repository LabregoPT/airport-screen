package model;

/**
 * Enum that holds every possible sorting type inside the program.
 * @author Jhon Edward Mora - Universidad ICESI - A00355710
 * @version 1.0 April 2019
 */
public enum Sortings {
	TIME, AIRLINE, DESTINATION, FLIGHT_NUMBER, BOARDING_GATE;
}
